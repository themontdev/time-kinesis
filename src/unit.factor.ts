export enum unitFactor {
    millisecond = 1,
    milliseconds = millisecond,
    second = millisecond * 1000,
    seconds = second,
    minute = second * 60,
    minutes = minute,
    hour = minute * 60,
    hours = hour,
    day = hour * 24,
    days = day,
    week = days * 7,
    weeks = week
};